apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: worldcup
  labels:
    app: worldcup-app
spec:
  type: NodePort
  selector:
    app: worldcup-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
